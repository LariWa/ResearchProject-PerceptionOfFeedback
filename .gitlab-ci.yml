pages:
  image: node:latest
  stage: deploy
  script:
    - cd hello-world
    - npm install --progress=false
    - npm run build
    - rm -rf public
    - mkdir public
    - cp -r dist/* public
  artifacts:
    expire_in: 1 day
    paths:
      - hello-world/public
  only:
    - main
